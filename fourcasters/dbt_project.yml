name: fourcasters
version: "1.0.0"
config-version: 2
profile: fourcasters

model-paths: ["models"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fourcasters:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
